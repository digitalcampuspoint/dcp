const toggleBtn=document.getElementById("mobile-menu"),navMenu=document.querySelector(".nav-links"),slides=document.querySelectorAll(".slide");let slideIndex=0;function rotateSlides(){0!==slides.length&&(slides.forEach(e=>e.classList.remove("active")),slides[slideIndex=(slideIndex+1)%slides.length].classList.add("active"))}toggleBtn&&navMenu&&toggleBtn.addEventListener("click",()=>{navMenu.classList.toggle("active")}),slides.length>0&&setInterval(rotateSlides,4e3);
